{
  "name": "org.asyar.speedtest",
  "version": "1.0.0",
  "description": "Measure your internet connection. Live gauge, plain-English verdict, and history chart.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest",
    "test:run": "vitest run",
    "publish": "asyar publish",
    "link": "asyar link",
    "package": "npm run build && zip -r extension.zip dist manifest.json"
  },
  "dependencies": {
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "asyar-sdk": "workspace:*",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@types/rollup-plugin-css-only": "^3.1.2",
    "typescript": "^5.0.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0",
    "@vitest/coverage-v8": "^2.1.0",
    "rollup-plugin-css-only": "^4.5.2"
  },
  "optionalDependencies": {
    "@rollup/rollup-win32-arm64-msvc": "*"
  }
}
